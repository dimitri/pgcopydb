# pgcopydb Environment Configuration
#
# Copy this file to .env and update with your database credentials.
# IMPORTANT: Add .env to .gitignore and set permissions with: chmod 600 .env
#
# Load with: set -a && source .env && set +a

# Database Connections (Required)
PGCOPYDB_SOURCE_PGURI="postgres://username:password@source-host:5432/sourcedb"
PGCOPYDB_TARGET_PGURI="postgres://username:password@target-host:5432/targetdb"

# SSL Configuration (Recommended)
PGSSLMODE="require"

# Performance Tuning (Optional)
PGCOPYDB_TABLE_JOBS=8
PGCOPYDB_INDEX_JOBS=2

# Working Directory (Optional)
# TMPDIR=/var/run/pgcopydb
